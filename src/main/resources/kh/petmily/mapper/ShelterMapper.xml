<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kh.petmily.mapper.ShelterMapper">

    <!--=======BasicMapper 메소드=======-->
    <select id="selectByPk" resultType="kh.petmily.domain.shelter.Shelter">
        select *
        from SHELTER
        where SNUMBER = #{pk}
    </select>

    <insert id="insert">
        insert into SHELTER(GROUPNAME, LOCATION, PHONE)
        values (#{groupName}, #{location}, #{phone})
    </insert>

    <update id="update">
        update SHELTER
        set GROUPNAME = #{groupName}, LOCATION = #{location}, PHONE = #{phone}
        where SNUMBER = #{sNumber}
    </update>

    <delete id="delete">
        delete from SHELTER
        where SNUMBER = #{pk}
    </delete>
    <!--=======BasicMapper 메소드=======-->

    <select id="selectCount" resultType="int">
        select count(*)
        from SHELTER
    </select>

    <select id="selectIndex" resultType="kh.petmily.domain.shelter.Shelter">
        select * from
        (select ROWNUM as NUM, A.* from
        (select * from SHELTER order by SNUMBER desc) A)
        where NUM between #{start} and #{end}
    </select>

    <select id="selectAll" resultType="kh.petmily.domain.shelter.Shelter">
        select *
        from SHELTER
        order by SNUMBER
    </select>
</mapper>